source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

gem 'web3-eth', :git => 'https://github.com/julpi/web3-eth.git'
gem 'eth'

# Specify your gem's dependencies in loc-eth.gemspec
#gemspec
